export const environment = {
  production: true,
  apiUrl: '/api', // Use relative path if using Nginx reverse proxy or full URL for domain
  wsUrl: '/', // WebSocket URL relative to root
  osrmUrl: 'https://router.project-osrm.org/route/v1/driving'
};
